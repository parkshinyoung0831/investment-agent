# 보안 정책 — 취약점 신고와 이 저장소의 위험 표면

## 신고 방법

**공개 이슈·PR·토론에 취약점을 적지 마세요.** 저장소 관리자에게 비공개로 알립니다.

신고에 넣을 것:

- 영향받는 구성요소 (예: `execution/brokers/toss`, `notifications/channels/discord.py`)
- 재현 절차
- 실제 영향 — 무엇을 읽거나 바꿀 수 있게 되는지
- 알고 있다면 완화 방안

신고에 **넣지 말 것**: 실제 토큰·API key·계좌 번호·브로커 자격증명·개인 식별 정보.
재현에 자격증명이 꼭 필요하면 값을 빼고 "어느 환경변수가 관여하는지"만 적습니다.

유효한 신고는 접수를 확인하고, 조사한 뒤, 수정이 준비된 다음에 공개 시점을 조율합니다.

## 이 저장소에서 특히 민감한 곳

일반적인 웹 취약점보다 아래 넷이 이 시스템의 실제 위험입니다.

| 표면 | 무엇이 걸려 있나 | 지키는 장치 |
|---|---|---|
| 실주문 경로 | 실제 자금 이동 | `LIVE_ENABLED`·`TOSS_LIVE_ENABLED`는 **사람만** 켠다. 킬 스위치와 hard risk limit은 `trading/risk/gate.py`가 코드로 소유한다 |
| Discord 승인 | 승인 위조 시 주문 실행 | 승인은 1회 소비(`consumed_at`), 만료 검사, 버튼 외 입력 거절 |
| Supabase service key | 전 스키마 읽기·쓰기 | 대시보드는 `SelectOnlyGateway`만 통과. RPC allowlist는 비어 있어 전부 거부 |
| 자격증명 유출 | 계좌 접근 | `.env`는 `.gitignore` 차단, 비밀값은 GitHub Actions secrets로만 주입 |

### 자동 판단 계층에 대해

LLM은 **근거를 모으고 후보를 설명**하는 데까지만 관여합니다. hard risk limit, 최종 포트폴리오
비중, broker 실행 권한은 LLM에게 넘기지 않습니다. 프롬프트 주입으로 위험 한도가 느슨해지는
경로가 있다면 그것은 설계 결함이며 보안 신고 대상입니다.

## 신고가 아닌 것

- 라이브 거래를 켜는 방법 문의 — 문서에 있고, 사람이 명시적으로 켜는 것이 의도된 설계입니다.
- 백테스트 결과나 전략 성과에 대한 이견.
- 공개 데이터 제공자(SEC·FRED 등)의 데이터 오류 — 그쪽에 알려야 합니다.

## 이 저장소를 직접 운영한다면

- 자격증명을 저장소가 아니라 환경변수·비밀 저장소에 둡니다.
- `LIVE_ENABLED`는 검증이 끝난 뒤에만 켭니다. 기본값은 꺼짐이며, 모르는 값은 꺼진 것으로
  읽습니다(fail-closed).
- 공개 배포 전 점검 항목은 [오픈소스 공개 준비도](docs/OPEN_SOURCE_READINESS.md)에 있습니다.
